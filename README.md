
  # Design homepage with specified colors

  This is a code bundle for Design homepage with specified colors. The original project is available at https://www.figma.com/design/dVpfn5EbmCCck4L0vudOAU/Design-homepage-with-specified-colors.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  